## Packages
framer-motion | For smooth page transitions and checkout steps animations
lucide-react | Iconography
embla-carousel-react | For product and testimonial carousels
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Images are served from /images (dynamic) or Unsplash (static).
CPF validation logic needed in checkout.
